export class EdgePopup{
    constructor(config = {}){

        //set element
        //1. cerco se c'è un elemento con classe popup
        //1.2 Se true => aggiungo classe popup-edge
          //*popup-edge dovra' rimuovere le proprietà specifiche per la visualizzazione mobile e quelle "limitanti di bootstrap"
    }
}

/*
* config = {
*   breakpoint: Number  -> serve per verificare se mostrare o meno l'edgePopup
*   isBootstrapModal: Boolean
*
* }
*
* */